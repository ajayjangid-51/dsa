#include <bits/stdc++.h>
using namespace std;

#define ll long long
#define mp make_pair
#define f first
#define s second
#define pb push_back
#define debug(x) cout << #x << " = " << x << endl;
#define debugpair(x, y) cout << #x << " = " << x << " , " << #y << " = " << y << endl;
#define fo(i, start, n) for (int i = start; i < n; i++)
#define scani(x) scanf("%d", &x)
#define scanlongd(x) scanf("%lld", &x)
#define all(x) x.begin(), x.end() // this will resolve to this..
#define sortall(x) sort(all(x))
#define mod 1000000007

typedef vector<int> vi;              // "i" ko read krna hai "int"
typedef vector<string> vs;           // prefix meh "s" ko read krna hai "string"
typedef pair<int, int> pii;          // "ii" ko read krna as "int-int" and i , s se jo phle hai usko read as "x" of i or s or ii like this...
typedef vector<pair<int, int>> vi2d; // vector-of-int-2d
typedef map<int, int> mpii;          // read "mp" as "map" // thus toh this overall will be called map-of-int-int
typedef set<int> si;                 // as suffix meh "s" ko read krna hai "set". toh yeh allover banjayehga "set-of-int"

int main()
{
    return 0;
}
